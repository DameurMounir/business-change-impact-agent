# Impact Room

The local Streamlit interface presents overview metrics, direct impacts, indirect impacts, conditional impacts, collisions, exact canonical traces and the digest-bound review workflow. It uses the packaged synthetic case and provider-free rules. No network provider or production data is required.

Launch with:

```bash
uv run streamlit run streamlit_app.py
```
