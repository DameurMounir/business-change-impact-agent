# Export contract

JSON, Markdown and standalone HTML are generated from one canonical snapshot. Each format carries the same SHA-256 snapshot digest. Draft exports are labelled `DRAFT_NOT_APPROVED`; terminal confirmations are labelled `CONFIRMED_HUMAN_REVIEW`. The HTML has inline styles, escapes evidence-derived text and requires no external CDN.
