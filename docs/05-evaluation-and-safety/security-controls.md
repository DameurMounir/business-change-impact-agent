# Security and safety controls

The release gate covers path traversal, symlinks, case and manifest tampering, graph cycles, forbidden reversal, maximum depth, missing evidence, unknown entities and relationships, prompt-like evidence, evaluator leakage, stale review, nonce expiry and replay, concurrent confirmation, ledger tampering, HTML escaping, package contents, static analysis, secret scanning and dependency audit.
